{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h4>Contactez - Nous</h4>
        <hr>
        <div class="contact-content">
            <div class="form_row">
                {{ form_start(formContact) }}
                <div class="col">
                    <div>
                        {{ form_row(formContact.lastName, {'label':'Prénom'|trans}) }}

                    </div>
                    <div>
                        {{ form_row(formContact.firstName, {'label':'Nom'|trans}) }}

                    </div>
                </div>
                <div class="col">
                    <div>
                        {{ form_row(formContact.email, {'label':'Adresse Electronique'|trans}) }}

                    </div>
                </div>
                <div class="col">
                    <div>
                        {{ form_row(formContact.object, {'label':'Objet du message'|trans}) }}

                    </div>
                </div>
                <div class="col">
                    <div>
                        {{ form_row(formContact.message, {'label':'Message'|trans}) }}

                    </div>
                </div>
            </div>
            <input type="submit" class="btn btn-primary center" value="{% trans %}Envoyer{% endtrans %}">
            {{ form_end(formContact) }}

        </div>
        </div>

{% endblock %}