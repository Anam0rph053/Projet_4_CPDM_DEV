{% extends 'base.html.twig' %}
{% block content %}

            <div class="container">
                <div class="row">
                    <div class="breadcrumbs">
                        <div class='inner' id="sub-header">
                            <ul id="cf">
                                <li><a {% if step >= 1 %} href="{{ path('home') }}" {% endif %}><i class="fa fa-home" aria-hidden="true"></i><span class="title_crumbs">{{ 'Accueil'|trans }}</span></a></li>
                                <li><a {% if step >= 2 %} href="{{ path('info') }}" {% endif %}><i class="fas fa-feather-alt" aria-hidden="true"></i><span class="title_crumbs">{{ 'Tickets'|trans }}</span></a></li>
                                <li><a {% if step >= 3 %} href="{{ path('recap') }}" {% endif %}><i class="fa fa-cart-plus" aria-hidden="true"></i><span class="title_crumbs">{{ 'Résumé'|trans }}</span></a></li>
                                <li><a {% if step >= 4 %} href="{{ path('confirm')}}" {% endif %}><i class="fas fa-flag-checkered" aria-hidden="true"></i><span class="title_crumbs">{{ 'Paiement'|trans }}</span></a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
{% endblock %}